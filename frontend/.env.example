# Dev-server proxy target for /api, /graphql and /ws (read by vite.config.ts only).
HPMS_BACKEND_URL=http://localhost:8000

# Leave empty to call the API on the same origin (dev proxy or nginx in production).
VITE_API_BASE_URL=
